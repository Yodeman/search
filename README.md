# search
Given a starting directory, this script searches for the keyword in the specified files inside the directory. It traverses through all the folders in the directory, searching for the keyword.
